---
"bloodhound-cli": minor
---

Improve provider configuration UX by requiring provider-specific named flags (for example `--consumer-key`, `--consumer-secret`) and add `config requirements <provider>` for discovery.
